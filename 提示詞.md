完全優化這個專案，把代碼都放入/scr
我的期望:

請幫我開發一個名為「Gemini 復刻版（Gemini Clone-Agent）」的全功能 Web 應用程式。 此平台以 Google Drive 為核心工作區，並整合多模態 AI 工具箱，實現全能型 AI 代理人。

🛠️ 核心基礎架構（Core Infrastructure） Google Drive 自動化工作區

連結使用者的 Google Drive 作為預設檔案庫。 支援 Agent 自動化操作。例如： 「幫我壓縮我的文件發給[名稱/信箱]」：自動打包指定資料夾並透過 API 發送。 「[名字]的電話是多少」：自動檢索雲端硬碟中的通訊錄或文件。 「寫個簡單的 scratch」：直接在工作區內生成程式碼檔案。 多 API 整合與路由中心（User-Defined API Gateway）

提供前端後台，讓用戶自訂與切換多種 AI 服務的金鑰（API Keys）與搜尋引擎。 支援清單：Groq (極速推論)、Gemini 系列 (大上下文多模態)、DuckDuckGo/Google (網路搜尋) 等。 🖥️ 功能模組開發需求

簡潔版 AI Studio（智能網頁工具） AI 瀏覽器模式：用戶輸入關鍵字提示詞（例如：「幫我找 micro:bit V2 規格...」），系統自動呼叫搜尋引擎，由 AI 篩選並「自動歸類整理」搜尋結果，呈現最精準的結構化連結清單。 「你輸入你猜猜」模式（網頁解讀）：使用者僅輸入一個 URL 網址（例如：輸入一個複雜的網址），AI 自動抓取該網頁內容，主動理解並由 AI 生成核心摘要。
多模態環境感知（Vision & Location） 螢幕與鏡頭分析：整合 WebCam 與螢幕截圖 API。 支援指令：「螢幕上這是什麼？」、「對於這個你有什麼看法？」、「鏡頭中這是哪裡？」。 系統需具備視覺辨識、OCR 與物件偵測能力，並給出深度評論。 空間感知（地理定位）：整合定位與地圖 Data。 支援指令：「我在哪裡？」，自動獲取 GPS 座標並回傳周邊地標與詳細環境解析。
高等數學與圖片推理模式（Advanced Math & Reasoning） 高等數學模式：內建符號運算與思維鏈（CoT），專門處理複雜數學與哲學邏輯。 圖片推理/生成模式：支援多模態圖像推理與生成。例如輸入一組歷史人物照片，執行「這些人小時候長怎樣？」的圖像生成與變老/變年輕演算法。 🧰 工具箱外掛組件（Microservices & Feature Icons） 請在系統架構中為以下功能設計標準化的外掛接口（Plugins），並在前端對應其功能圖示：
audio_spark (Convert text to speech)：將文字結果即時轉換為自然語音輸出。 music_note (Generate music)：串接音樂生成 AI 模型（如 Suno/Udio API）。 database_auth (Add database and auth)：整合輕量後端（如 Supabase/Firebase）提供帳號驗證與個人資料庫。 image_edit_auto (Create & edit images)：支援 Text-to-Image 與圖像局部修改、重繪。 audio_spark (Add voice conversations)：低延遲的雙向語音即時聊天（Voice Chat）。 movie (Animate images into video)：將靜態圖片動態化，生成短影片。 google (Use Google Search data)：調用 Google Search API 獲取即時資料。 google_pin (Use Google Maps data)：調用 Google 地圖資料進行位置解析。 image (Generate high-quality images)：調用高畫質圖像生成模型（如 Imagen / Midjourney / FLUX）。 spark (Add Gemini intelligence)：一鍵切換回 Gemini 原生智慧大腦核心。 voice_chat (Add a Gemini chatbot)：獨立的 Gemini 聊天機器人分流視窗。 video_spark (Generate video from text)：輸入提示詞直接生成全新短片。 aspect_ratio (Control image aspect ratios)：精準控制生成圖像的比例（如 16:9, 1:1, 4:5）。 document_scanner (Analyze images)：深度分析圖片中的表格、發票、手寫字並進行結構化輸出。

這是一個aistudio專案，推薦一個可行的營運商